% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/f_funcs.R
\name{pulse_cyc}
\alias{pulse_cyc}
\title{Pulse Wave Sequence}
\usage{
pulse_cyc(inputs = seq(0, 1, length.out = 9)[-9], duty_cycle = 0.5)
}
\arguments{
\item{inputs}{A numeric input vector (period 1 by default)}

\item{duty_cycle}{A numeric value in (0, 1) specifying the fraction of the period where the pulse is 1 (default 0.5)}
}
\value{
A numeric vector of pulse wave values (0 or 1)
}
\description{
Generates a rectangular pulse wave sequence for use as periodic coefficients in series analysis.
Returns 1 for the duty cycle portion of each period 1, 0 otherwise.
The default excludes the endpoint to prevent duplicate values when tiling.
}
\examples{
pulse_cyc()
pulse_cyc(seq(0, 1, length.out = 17)[-17], duty_cycle = 0.25)
pulse_cyc(seq(0, 2, length.out = 20)[-20], duty_cycle = 0.75)  # 2 periods

}
